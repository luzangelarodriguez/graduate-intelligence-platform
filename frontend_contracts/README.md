# Frontend Contracts

This folder contains the exported OpenAPI schema and example JSON payloads used by the Vercel frontend.

- `openapi.json`
- `contracts.json`

Generate them with:

```powershell
python scripts/export_api_contracts.py
```

